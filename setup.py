from setuptools import setup

setup(
    name='data_checker',
    version='0.1',
    description='A tool to apply checks to a dataset and generate a report.',
    author='Your Name',
    author_email='your-email@example.com',
    packages=['data_checker'],
    install_requires=[
        'pandas',
    ],
)

